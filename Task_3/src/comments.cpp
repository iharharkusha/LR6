/*
    char text[] = "hello, world! it1 is2 brawler_ andreey("; 
    char text2[] = "ahyel#12345ha";
    char text3[] = "hello, ivan name your 2132&dog $pls";
    char text4[] = "hello, brother. My name is ALexey, nice to meet you";
    f(text);
    f(text2);
    f(text3);
    f(text4);
    */

/*
std::cout << "\nbetween_seq: ";
                for (unsigned int i = 0; i < between_sequence_size; ++i) {
                    std::cout << between_seq[i];
                }
                std::cout << '\n';

std::cout << "\nseq:\n"; 
                for (unsigned int i = 0; i < diff; ++i) {
                    std::cout << seq[i]; 
                }
                std::cout << '\n'; 
                std::cout << "\nword2:\n"; 
                
                 for (unsigned int i = 0; i < w_size2; ++i) {
                    std::cout << word2[i]; 
                }
                std::cout << '\n'; 
                std::cout << '\n'; 
     

                std::cout << "\nword2:\n"; 
                
                 for (unsigned int i = 0; i < w_size2; ++i) {
                    std::cout << word2[i]; 
                }
                std::cout << '\n'; 
                std::cout << '\n'; 
*/

/*
for (unsigned int i = 0; i < cnt; ++i) {
        std::cout << *w_ptrs[i] << '\t'; 
    }
    std::cout << '\n';
    for (unsigned int i = 0; i < cnt; ++i) {
        std::cout << w_sizes[i] << '\t'; 
    }
    std::cout << '\n';

    char* text_ptr12 = text;
    while (*text_ptr12 != '\0') {
        std::cout << *text_ptr12;
        text_ptr12 += 1;
    }

    std::cout << '\n';

 */